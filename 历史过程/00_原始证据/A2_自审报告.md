# A2 原账号自我审计报告

## 1. 检查了什么

- 完整回查了 `A2_Codex_证据与决策.md` 与 `A2_来源索引.md`。
- 对照当前第二个 Codex 账号可访问的 post-handoff 原始对话，核对了时间顺序、用户反馈、实际修改、验收结论和当前文件状态。
- 复核了 `INHERITED_CONFIRMED` 与 `NEW_CONFIRMED` 的来源边界，检查是否把 handoff 内容重复登记为 A2 新发现。
- 复核了 evidence block、caption／正文分工、具体 Fig. 引用、`real-time feasibility criterion`、`t_{\mathrm{comp}}^{\max}` 与最终 claim boundary。
- 复核了关键纠错链：`we clarify...`、`the criterion`、`differ from those in the manuscript`、evidence block、caption citation、`panels (c)`、连字符微调、指标更名和 claim 收束。
- 复核了来源职责，重点区分 TIE PDF、三份总结、handoff、JAS、当前稿件、最终正式结果图、用户原始对话和 Nature Skill。
- 核对了当前 TeX、PDF、`[XX]` 与正式图文件的实际状态；确认本轮未修改 Comment 1 response。

## 2. 修正了什么

1. 新增并正式使用了两个状态标签：
   - `CASE_SPECIFIC`：标识当前 Comment 1、reviewer、稿件、图形、数值、caption、引用和文件状态等不可直接泛化的裁决。
   - `SUPERSEDED`：标识已被后续用户裁决替代、只用于保存历史链的早期术语与判断。

2. 明确补充：`INHERITED_CONFIRMED`／`NEW_CONFIRMED` 只表示在相应范围内已确认，不自动等于可进入通用知识库。

3. 将以下内容明确标为 `CASE_SPECIFIC`：
   - Cases 1/2 与 `W_x/S_e(t)` 的当前 evidence-block 分组；
   - 当前项目的变量、数值、图号、caption citation 和文献对应；
   - `panels (c)` 改为具体 Figs.；
   - `real-time-feasibility criterion` 的当前句子微调；
   - `maximum online computation time per control step ($t_{\mathrm{comp}}^{\max}$)` 及判据；
   - 当前 tested tasks／reported variations 下的 claim boundary。

4. 将 handoff 中的旧指标名称 `maximum total computation time per step` 标为 `SUPERSEDED`，并将其从一般性 `REJECTED` 调整为“当前项目中已被替代的历史术语”。

5. 将 handoff 中“明确实时性主要收束于 JAS Cases 1/2”的较窄状态标为 `SUPERSEDED`；现行结论覆盖 two tested interaction tasks 与 reported `W_x/S_e(t)` variations，但仍严格排除未测试条件和 universal guarantee。

6. 强化了 evidence-block 的最终校准：同一 block 内共同目的和公共设置集中说明，不同实验不可替代的设置、数值和结果必须保留；未再使用“所有设置只交代一次”的简化表述。

7. 为九个关键案例补全或强化了纠错链，记录用户反馈、A2 修改、复核原因、来源依据和最终状态。

8. 明确 caption citation 是当前项目裁决：caption 自足、正文高密度成立，但“不在当前 captions 重复 IL-SOGP 引用”不能泛化成所有期刊规则。

9. 将“未虚构 revised-manuscript 位置”从 `UNRESOLVED` 移到已确认项目边界；它是已遵守的限制，不是待解决问题。

10. 将原先“真正可迁移的新认知”改为“提交三源交叉裁决的候选抽象点”，并明确具体指标名和完整 Fig. 引用仍属 `CASE_SPECIFIC`。

11. 修正了来源索引：
    - 新增 post-handoff 用户原始对话的独立职责；
    - 新增最终正式结果图作为新增比较数值、曲线关系和图示判据的项目事实来源；
    - 明确 JAS 只负责 Cases 原始事实，不能替代新增 comparison figures；
    - 明确当前稿件只负责本稿正式变量、比较对象和既有事实；
    - 将 TIE PDF 与三份总结拆开，明确总结只能辅助定位和诊断；
    - 明确 Nature Skill 只辅助 claim/source discipline，不决定 TIE 风格；
    - 将原“只能由 TIE／总结支持”等合并表述改为不可替代的来源职责矩阵。

## 3. 确认无需修改的内容

- handoff 的四类继承内容及其 A1 来源属性保存准确，没有被重写成 A2 独立发现。
- A2 post-handoff 的主要时间线完整：独立验收与 16 项复核、整体收敛、减法、三处定点修改与编译、evidence-block 重组、指标更名、二次校准和最后两处定点修改。
- proposed computation time 一般高于 IL-SOGP 的不利事实已如实保留。
- 当前 claim 只覆盖 tested tasks／reported variations，没有恢复 minimum computation time、lowest cost 或 universal guarantee。
- evidence block 中 Case-specific `C`、全部 `\mathcal J_{10}` 数值、4.75/4.65 ms、`W_x/W_f` 和三个 `S_e(t)` 设置均保存完整。
- `REJECTED` 错误历史仍保留，没有因自审而删除。
- `[XX]` 未被擅自替换；图文件未被修改；Comment 1 response 未被改动。

## 4. 仍然真实存在的 `UNRESOLVED`

1. JAS 文献在 revised manuscript 中的 `[XX]` 仍需作者最终确定。
2. 当前 TeX 晚于现有 PDF，最新版 Comment 1 尚未重新编译同步。
3. 最新版尚未进行编译后的视觉验收，包括长 subcaption、分页和图文距离。
4. 先前 16 项问题清单及逐项裁决原文未完整保存在当前可访问的压缩对话中，不能凭推测重构。
5. 另一窗口处理后的图文件最终替换和视觉状态未由 A2 原账号验收；本次只确认当前 TeX 调用的正式 PDF 文件均存在。

## 5. 是否可以进入“三源交叉裁决”

可以。

前提是后续裁决继续保留以下边界：

- `CASE_SPECIFIC` 不自动抽象为通用规则；
- `SUPERSEDED` 只用于历史链，不与现行判断并列执行；
- `REJECTED` 不转写为正向规则；
- `UNRESOLVED` 不通过推测补全；
- 涉及 TIE 风格的抽象必须同时回到 TIE PDF、用户最终裁决和相应事实来源，而不能只依据 A2 当前英文或一般 academic-writing 偏好。
