# 项目特例与事实池

> 本文件集中保存 `CASE_SPECIFIC` 与 `SOURCE_FACT_ONLY`。它们可在未来作为带边界的案例或正反例，但不得直接进入通用知识库。具体数值、变量、图号、citation 编号和文件状态均保持项目属性。

## A. `CASE_SPECIFIC`

### CS-01（CI-038）：Comment 1 的主论证线

- reviewer 质疑：上一版只改变环境刚度并给 task-cost curves，不能证明不同任务/设置下的 computational-efficiency 结论。
- 当前回答顺序：先界定真实计算目标和评价尺度，再补充不同 interaction tasks 与不同 settings 下的 task cost + online computation time。
- 项目边界：只处理 Comment 1，不扩展实验、不改其他 comments。
- Provenance：A1 原始作者思路/用户裁决；A2 handoff 继承；A3 再确认。

### CS-02（CI-039）：当前正式指标与判据

- 正式名称：`maximum online computation time per control step`。
- 正式符号：`t_{\mathrm{comp}}^{\max}`。
- 当前判据：`t_{\mathrm{comp}}^{\max}<\Delta t`。
- 项目边界：这是本稿评价对象和符号，不是其他论文必须采用的名称。
- Provenance：A2 post-handoff 用户正式更名；A3 再确认；当前图/TeX 支持。

### CS-03（CI-040）：当前两个 evidence blocks

1. JAS Cases 1/2 作为“不同 interaction tasks”证据块。
2. `W_x` 与 `S_e(t)` variations 作为原 two-DOF task 中“不同 objectives/settings”证据块。

- 公共目的/设置在 block 内集中；各实验独有设置、数值和结果保留。
- 项目边界：两个 block 的数量与内容完全依赖当前 Comment 1。
- Provenance：A2 A2-06/A2-07；A3 §4；用户最终裁决。

### CS-04（CI-041）：具体 Fig.-panel references

- 当前 Cases 结果句由含糊的 `panels (c)` / `Panels (d)` 改为两张 figures 的具体 `(c)` / `(d)` 引用。
- 项目边界：只解决当前两图并列时的指代问题，不要求所有 response 逐一列全图号。
- Provenance：A2 A2-08/NC-09；A3 §5.2/§11.3。

### CS-05（CI-042）：当前短语的连字符微调

- 当前使用 `real-time feasibility criterion`。
- 被替换文本：`real-time-feasibility criterion`。
- 项目边界：定点语言裁决，不是 TIE 特有标点通则。
- Provenance：A2 A2-08/NC-14；A3 §11.2。

### CS-06（CI-043）：当前 captions 的 citation 处理

- 正文首次引入 IL-SOGP 时已有正式引用。
- 当前 captions 不为“形式统一”重复添加 revised-manuscript citation 编号。
- 当前图内 `IL-SOGP [10]` 是另一项已确认的图形决定，不等于 caption 通则。
- 项目边界：citation 重复需求随期刊、图自足程度和上下文变化。
- Provenance：A2 NC-10；A3 §11.1；TIE 重核与用户裁决。

### CS-07（CI-044）：当前 reviewer 阅读路径与编号分点

- 内部判断：该 reviewer 主要扫正式结果图、caption 和图前后几句，并带着“最低计算成本”的先入理解审视证据。
- 当前设计：核心纠正和最小认知框架前置；两个独立关切用 `1)` / `2)` 分点；图附近放置可独立理解的核心文字。
- 项目边界：TIE 本身不稳定使用编号；此阅读假设不能泛化到所有 reviewer。
- Provenance：A1 `本审稿人阅读习惯.txt` 及用户裁决；A2 继承；A3 §5.3/§13.2。

### CS-08（CI-045）：当前两套文献编号体系

- response 自身 citation 与 revised manuscript 编号是两套语境。
- lattice-distributed RBFNN 在稿件中的已知对应编号为 `[29]`，并在 response 中有正式 bibliography item。
- 图内 `IL-SOGP [10]` 属 response 文献体系。
- JAS 的 revised-manuscript 编号仍为 `[XX]`，不得猜测。
- Provenance：A1 §4.5/§7.3；A2 NC-12；A3 §16.2。

### CS-09（CI-046）：当前图内文字、条件与对齐

- Figure 3 权重条件：`W_x:W_f=20:1, 50:1, 200:1`。
- Figure 4 刚度使用 `S_e(t)` 和 `I`，不使用 `S_e`、`I_2` 或 `S_{e,\max}`。
- timing panels 右上角显示与对应 task-cost panel 一致的 condition；control-period 标注按当前配对图要求放置。
- `IL-SOGP` 后按用户要求显示 `[10]`。
- 修改边界：不改数据、曲线、轴范围、虚线、图例顺序/含义、布局、尺寸、分辨率或比较关系。
- Provenance：A1 §7；A3 §12；A2 明确图由另一窗口处理。

### CS-10（CI-047）：当前 claim boundary

- 可支持：two tested interaction tasks 与 reported `W_x/S_e(t)` variations 下满足 `t_{\mathrm{comp}}^{\max}<\Delta t`。
- 同时保留：proposed method 的 computation time 一般高于 IL-SOGP。
- 不可支持：所有任务、平台、采样周期上的普遍实时保证，或相对所有方法最低 computation time。
- Provenance：A2 NC-11/NC-13；A3 §10；最终正式图提供事实支撑。

### CS-11（CI-048）：当前版本的验收与修改边界

- A1 handoff 时：核心逻辑已确定，但英文尚未最终验收。
- A2-05：用户明确判断当时版本整体通过内容、逻辑和表达验收。
- 后续：只执行用户点名的 evidence-block、指标更名、具体 Fig. 引用和连字符等定点修改，不再泛化式润色。
- 边界：写作验收不等于最新版 TeX 已编译、视觉已验收或 `[XX]` 已解决。
- Provenance：A1 handoff 状态；A2 用户最终验收；A3 §2.3/§11.4。

## B. `SOURCE_FACT_ONLY`

### SF-01（CI-049）：JAS Cases 的事实

- JAS 论文：`Robot Impedance Iterative Learning with Sparse Online Gaussian Process`。
- Case 1：accurate tracking；`\alpha=10`；`C=[25,0,1]`。
- Case 2：compliant interaction；`\alpha=20`；`C=[100,0,0.1]`。
- 平台：simulated 7-DoF Franka Emika Panda。
- outer-loop sampling delay/control period：10 ms；inner loop 1 ms（A1 来源记录）。
- 职责：只核实任务性质、平台和原始设置；不决定 response 风格、本稿 `W_x/W_f/S_e(t)` 或新增 comparison figures 的全部数值。
- Primary source：JAS 原论文 PDF。

### SF-02（CI-050）：当前稿件的事实

- 正式变量：`S=[W_x\;0\;W_f]`；`W_x` 对应 position/tracking term，`W_f` 对应 force-integral term；环境刚度使用 `S_e(t)`。
- 比较口径：fast-AGP vs standard GP；CRBFNN vs lattice-distributed RBFNN。
- 原机器人实验：proposed method 9.90 ms，control period 20 ms；standard GP 在相同条件下超过 control interval。
- 文献事实：lattice-distributed RBFNN 在 revised manuscript 中对应 `[29]`。
- 职责：核实本稿变量、比较对象、既有事实和文献对应；不决定 TIE 风格或新增图中的全部比较结果。
- Primary source：当前稿件 `root.tex` / 指定版本稿件。

### SF-03（CI-051）：最终正式结果图的事实

- Case 1 `J_10`：proposed 80.40、IL-SOGP 86.01、classical IL 88.61；proposed `t_comp^max=4.75 ms`。
- Case 2 `J_10`：proposed 64.83、IL-SOGP 66.86、classical IL 72.06；proposed `t_comp^max=4.65 ms`。
- 两个 Cases 中 proposed timing 高于 IL-SOGP、低于 10 ms control period。
- `W_x/S_e(t)` variations 图支撑各已报告设置中的 task-cost 与 online-computation-time 关系，包括 proposed `J_10` 低于 IL-SOGP、proposed timing 一般高于 IL-SOGP、并低于图中对应 control period 的关系。
- 职责：只支撑数值、曲线高低和图示判据；不定义任务、变量或写作方式。
- Primary source：作者指定的最终正式结果图 PDFs。

## 防污染说明

- CS/SF 项可以作为候选知识卡的 illustration，但引用时必须显式标注“当前项目实例”。
- 不得从本池抽取具体数字、变量、图号、citation 编号、文件状态或 reviewer-specific 阅读假设作为通用规则。
- `SOURCE_FACT_ONLY` 只能支撑当前案例的内容真实性，不能提高某条写作判断的通用可信度。

