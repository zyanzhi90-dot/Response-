# 未决事项

> 仅保存三源交叉后仍无法裁决的 `UNRESOLVED`。不得通过合理猜测补全，也不得在本阶段修改项目文件来消除这些未决项。

## U-01（CI-052）：revised manuscript 的 JAS citation `[XX]`

- **未决内容：** `IL-SOGP~\cite{respPanJAS2025} ([XX] in the revised manuscript)` 中 `[XX]` 的最终编号。
- **三源状态：** A1、A2、A3 均明确要求保留占位符，不得猜测。
- **已排除的伪冲突：** 图内 `IL-SOGP [10]` 属 response 文献体系，与 revised-manuscript `[XX]` 是两套编号语境，不构成冲突。
- **解决所需证据：** 最终 revised manuscript 的参考文献编号。
- **本阶段处理：** 保留，不修改。

## U-02（CI-053）：task-cost y-axis 的逗号

- **未决内容：** A1 记录用户曾要求“所有图的逗号去掉”，但当前正式图文本仍可抽取得到 `Task cost, J_j`；timing y-axis 已无逗号。
- **三源状态：** A2/A3 没有提供对这一字面差异的后续明确裁决。
- **不能采用的推断：** 不能因为用户后来授权替换整组候选图，就推定该逗号已被默许或已完成验收。
- **解决所需证据：** 用户对该轴文字的明确最终裁决或经确认的最终图版本。
- **本阶段处理：** 保留，不改图。

## U-03（CI-054）：最新版 TeX/PDF 同步与视觉验收

- **未决内容：** 最新 `Response-TCYB.tex` 是否已编译成同步 PDF，以及长指标名称、subcaption 换行、分页、图文距离和另一窗口最终图替换状态是否已做最终视觉验收。
- **已知事实：** A2-05 曾成功生成 15 页 PDF；但其后又进行了 Section 2 evidence-block 重组、指标更名、具体 Fig. 引用和连字符修正。现有 PDF 早于当前 TeX。
- **不能采用的推断：** 旧版本成功编译不能证明最新版同步；正式图文件存在也不能证明视觉终验完成。
- **解决所需证据：** 用户授权后的重新编译、渲染与视觉检查，或明确的后续验收记录。
- **本阶段处理：** 保留，不编译、不修改 response/图。

## U-04（CI-055）：A2 的“16 项问题清单”逐条原文

- **未决内容：** A2 压缩对话没有保存当时 16 项原始列表和逐项分类答复的全文。
- **可保留内容：** 用户要求的三类分类方法、后续明确保留的修改锚点、删除项不得回流和边界限制。
- **不能采用的推断：** 不得按后续印象重新制作“第 1–16 条”并声称是历史原文。
- **解决所需证据：** 原始未压缩对话或当时的完整导出。
- **本阶段处理：** 保留缺失状态，不重构。

## U-05（CI-056）：`rather than ...` 候选的精确最终英文

- **未决内容：** A1 记录 `rather than to achieve the lowest computational cost` 是比直接否定缓和的候选，但当时未获最终逐字验收。
- **后续已解决部分：** A2/A3 已解决更高层方向——当前项目不采用 `We do not claim ...` 作为默认/TIE 式开场，而以正面界定真实目标和证据推进。
- **仍不能确认部分：** A2/A3 没有保真记录该 `rather than ...` 字符串是否仍存在、是否被逐字批准或是否已被其他表达替代。
- **不能采用的推断：** 不能从“某版本整体通过”反推一个未确认是否仍在该版本中的精确字符串已经获批。
- **解决所需证据：** 对应最终版本文本与用户逐字裁决记录。
- **本阶段处理：** 仅保留为历史精确措辞未决；不影响已经裁决的正面 framing 方向。

## A1/A2 早期 `UNRESOLVED` 中已被后续来源解决的项目

以下项目不再留在未决池：

| 早期未决项 | 后续解决来源 | 当前归宿 |
|---|---|---|
| `We do not claim ...` 是否作为当前开场方向 | A2 正面证据式版本与用户整体验收；A3 再次排除默认 negative/meta opening | CI-022 `REJECTED`（只针对当前默认/TIE 式开场） |
| JAS Cases 的最小充分设置密度 | A2 evidence-block 二次校准；A3 明确“足以显示任务不同，但不复述 Methods” | CI-006 `GENERALIZABLE_CANDIDATE` + CI-040/CI-049 项目应用/事实 |
| 多个 purpose sentences 的重复密度 | A2/A3 将四个重复模板重组为 evidence blocks | CI-026 `REJECTED` + CI-005/CI-008 候选 |
| 权重/刚度图是否可进入实时性 claim | A2 正式图核验与用户验收；A3 再确认 | 早期窄边界 CI-037 `SUPERSEDED`；现行 CI-047 `CASE_SPECIFIC` |
| 图内 `[10]` 与稿件 `[XX]` 是否冲突 | A1 自审已区分两套编号；A2/A3 沿用 | 冲突已消除；仅 `[XX]` 编号本身留作 U-01 |
| 当前英文是否仍处于“完全未验收”状态 | A2-05 用户明确整体通过内容、逻辑和表达验收；后续只做定点修改 | CI-048 `CASE_SPECIFIC`；但最新版编译/视觉状态仍为 U-03 |

## 未从未决池删除的理由

- U-01/U-02 缺少用户或最终版本的明确裁决。
- U-03 是可验证的文件/视觉状态缺口，不能用写作验收替代。
- U-04 缺少原始历史文本，任何重构都会伪造 provenance。
- U-05 缺少精确字符串级裁决；高层 framing 已解决不等于逐字候选已解决。

